#!/usr/bin/env bash
set -Eeuo pipefail

if [ -f /etc/default/kexec ]; then
  sed -i 's/^[[:space:]]*LOAD_KEXEC=.*/LOAD_KEXEC=false/' /etc/default/kexec || true
fi

systemctl disable kexec.service 2>/dev/null || true
systemctl mask kexec.service 2>/dev/null || true
