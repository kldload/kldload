# Firecracker microVM counts for the estate dashboards. Oneshot, driven by
# kfire-exporter.timer — see the script for why the libvirt counters alone
# are not enough.
[Unit]
Description=Firecracker microVMs → Prometheus textfile exporter
[Service]
Type=oneshot
ExecStart=/usr/local/bin/kfire-exporter
