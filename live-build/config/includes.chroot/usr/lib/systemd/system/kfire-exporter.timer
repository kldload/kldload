# 15s, matching arcstats-exporter: fast enough that a demo cloning forty-five
# machines shows the counter climbing rather than stepping once at the end.
[Unit]
Description=Run kfire-exporter every 15s
[Timer]
OnBootSec=10s
OnUnitActiveSec=15s
AccuracySec=1s
[Install]
WantedBy=timers.target
