#!/bin/bash
# kldload-test — run smoke tests on this kldloadOS system
# Usage: sudo kldload-test [core|server|desktop|auto]

set -Eeuo pipefail
trap 'echo "kldload-test: FAIL at line $LINENO: $BASH_COMMAND" >&2' ERR

exec bash /usr/local/share/kldload/tests/smoke-${1:-auto}.sh
