#!/usr/bin/env bash
# Convenience forwarder — the real installer is /usr/sbin/kldload-install-target.
# This exists so the tool is reachable from PATH without a full sbin lookup.
exec /usr/sbin/kldload-install-target "$@"
