# Three-pin dock: Files, Chrome, sysdiag.
# Operator on .142 b644 2026-06-07 settled here after a series of
# layout iterations: Konsole stays in the grid (Super+A) for plain-
# shell launches; sysdiag (which itself opens IN Konsole with the
# tmux cockpit) is the pinned daily-driver terminal. Files + Chrome
# round out the trio. Every other launcher (kldload tools, bob-*,
# Ptyxis, ksnap, kexport) is NoDisplay=true via profiles.sh polish.
[org/gnome/shell]
favorite-apps=['org.gnome.Nautilus.desktop', 'google-chrome.desktop', 'kldload-sysdiag.desktop']
