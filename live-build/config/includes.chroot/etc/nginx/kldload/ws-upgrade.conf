# WebSocket upgrade headers — included from every location that may carry
# a WS upgrade (Grafana Live, ttyd terminals, Headlamp exec streams,
# future noVNC, etc.). The $connection_upgrade map is defined in nginx.conf
# at the http{} level.

proxy_set_header    Upgrade     $http_upgrade;
proxy_set_header    Connection  $connection_upgrade;
