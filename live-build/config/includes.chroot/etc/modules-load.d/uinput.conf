# kldload: load uinput at boot so Steam controllers / virtual input work.
# Without this, Steam (Flatpak) warns "udev rules not installed" and gamepad
# input via /dev/uinput is unavailable. Pairs with 71-uinput.rules (perms).
uinput
