# kldload Salt master configuration
# Enables the kldload_db external pillar so nodes get their service
# definitions from state.db when they run state.highstate

ext_pillar:
  - kldload_db: {}

# kldload state files location
file_roots:
  base:
    - /srv/salt

pillar_roots:
  base:
    - /srv/pillar
