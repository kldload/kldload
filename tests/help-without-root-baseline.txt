# Tools that re-exec as root and do not yet answer --help.
#
# A RATCHET: this list may only shrink. Giving a tool a --help handler that
# prints usage and exits 0 without root is the fix; then delete its line here
# and check-help-without-root.sh will hold it that way.
#
# The rule: --help answers first, needs no root, and exits 0.
# Seeded 2026-09-16 with 20 names. EMPTY as of the same day: all 32 tools that
# re-exec as root now answer --help without it. Keep it empty.
