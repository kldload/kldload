# GNOME desktop profile packages
# Individual packages instead of task-gnome-desktop to avoid gnome-snapshot
# → gstreamer1.0-plugins-bad → libfluidsynth3 → sf3-soundfont-gm dep failure
gnome-shell
gnome-session
gnome-control-center
gnome-settings-daemon
# Display manager: LightDM (NOT gdm3). GDM 48 on Debian Trixie has a
# systemd-integration bug where gnome-session-binary errors with
# "Unit name gnome-session-(null)@gnome.target is not valid" and GDM
# aborts with "GdmSession: no session desktop files installed" — even
# with libpam-gnome-keyring + libpam-systemd + xdg-desktop-portal-gnome
# all installed. LightDM bypasses systemd-managed session units, runs
# gnome-session via Xsession scripts, lands in working GNOME desktop.
# See profiles.sh _gdm logic + project_f44_cutover_findings.md.
lightdm
nautilus
gnome-terminal
gnome-text-editor
loupe
gnome-tweaks
file-roller
gnome-system-monitor
adwaita-icon-theme
fonts-cantarell
gvfs
gvfs-backends
gnome-keyring
# PAM modules — Debian splits these out from the parent packages.
# libpam-gnome-keyring provides /usr/lib/.../security/pam_gnome_keyring.so.
# libpam-systemd is required by user-session/cgroup integration.
# Without these, GDM autologin warns "PAM unable to dlopen" and session
# worker gets confused — fixing the underlying GDM 48 bug is upstream's
# problem, but we install these regardless so the future GDM-fixed
# Debian release works without re-darksiting.
libpam-gnome-keyring
libpam-systemd
# xdg-desktop-portal — Wayland portal backends GNOME 46+ needs to
# launch the user shell (file pickers, screenshots, screen sharing).
xdg-desktop-portal
xdg-desktop-portal-gnome
xdg-desktop-portal-gtk
# dbus-x11 provides /usr/bin/dbus-launch which gnome-session calls to
# bootstrap a session DBus when not under systemd-managed login.
# Without it, gnome-session-binary errors "Failed to execute child
# process dbus-launch (No such file or directory)" → session falls
# back to non-systemd path which then fails to upload environment to
# DBus → user logs in at lightdm but gets bounced back to greeter.
dbus-x11
firefox-esr
# Xorg fallback — Wayland fails on virtual GPUs (KVM, VMware)
xserver-xorg
xserver-xorg-video-qxl
xserver-xorg-video-vesa
desktop-base
fonts-liberation
fonts-noto-core

# Container runtime (required for Open WebUI)
# Docker on the apt distros, podman on the RPM ones — each ecosystem's own
# choice rather than one imposed on both. docker.io is in Debian main (22 MB
# over 17 packages) and does not conflict with podman, so both are present:
# docker for the containers a person came looking for, podman for the
# substrate's own. See lib/profiles.sh for the ZFS storage-driver wiring that
# makes every image layer a dataset.
docker.io
# docker-cli is a SEPARATE package in trixie. Installing docker.io alone gives
# /usr/sbin/dockerd and no `docker` command at all — verified on fiend,
# 2026-08-16: the daemon ran, the client did not exist.
docker-cli
docker-compose
podman
