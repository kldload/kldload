# Kubernetes node packages (APT — Debian)
# containerd from Debian repos, K8s from pkgs.k8s.io

# Container runtime
containerd
runc

# Networking prerequisites
conntrack
socat
nftables
ipset
ipvsadm
cri-tools

# Container networking
containernetworking-plugins

# SSH automation (kube-cluster cloud-init password fallback)
sshpass

# System tools
apt-transport-https
ca-certificates
curl
gpg
libseccomp2
# zram swap for the k8s HOST (a hypervisor, not a node): kubeadm needs swap
# off on NODES, and kube-setup already strips it inside every guest. Without
# this the host runs with zero swap and systemd-oomd has nothing to reclaim.
systemd-zram-generator
