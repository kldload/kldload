# ZFS packages for Arch Linux (archzfs repo)
# Prefer prebuilt zfs-linux (matched to a specific kernel version).
# Keep zfs-dkms as fallback for kernel version mismatches.
zfs-linux
zfs-dkms
zfs-utils

# Required for DKMS to build ZFS kernel modules (fallback)
dkms
base-devel
gcc
make
perl
kmod
linux-headers
