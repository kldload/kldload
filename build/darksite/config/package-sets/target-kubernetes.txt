# Kubernetes node packages (RPM — CentOS, RHEL, Rocky, Fedora)
# These are pulled from vendor repos + pkgs.k8s.io

# Container runtime
containerd.io

# Kubernetes core
kubelet
kubeadm
kubectl
cri-tools

# Networking prerequisites
conntrack-tools
socat
ipvsadm
iproute-tc
nftables

# Container networking
containernetworking-plugins

# Required kernel/system tools
ebtables
ipset
libseccomp

# Helm dependencies (installed separately via binary)
# helm is a static binary, not from RPM repos

# Monitoring (optional but recommended)
node_exporter
