# GNOME desktop profile packages
gnome-shell
gnome-session
gnome-control-center
gnome-settings-daemon
gdm
nautilus
gnome-terminal
gnome-text-editor
gnome-tweaks
file-roller
gnome-system-monitor
gnome-keyring
# gcr provides /usr/libexec/gcr-ssh-askpass which gnome-keyring file-requires.
# dnf5 doesn't auto-load filelists, so file-requires can't be satisfied via
# Provides resolution alone. Explicit install side-steps it. (bootstrap.sh
# also passes --setopt=optional_metadata_types=filelists as a broader fix.)
gcr
adwaita-icon-theme
google-noto-sans-fonts
# BOTH browsers ship. Chrome is not optional — kldload-chrome-app opens ten
# tool windows through it and Bob's voice needs the Chromium-only Web Speech
# API — so it stays the browser the system itself depends on. Firefox is the
# one an operator may simply prefer, and an image that pitches free software
# should not make Google's the only choice on the machine. Both are pinned;
# neither is hidden. (Reverses the Task #47 "Chrome only" call, 2026-08-10.)
firefox
mesa-dri-drivers
pipewire
wireplumber

# Container runtime (required for Open WebUI)
podman
