# GNOME desktop profile packages
gnome-shell
gnome-session
gnome-control-center
gnome-settings-daemon
gdm
nautilus
gnome-terminal
gnome-text-editor
gnome-tweaks
file-roller
gnome-system-monitor
gnome-keyring
# gcr provides /usr/libexec/gcr-ssh-askpass which gnome-keyring file-requires.
# dnf5 doesn't auto-load filelists, so file-requires can't be satisfied via
# Provides resolution alone. Explicit install side-steps it. (bootstrap.sh
# also passes --setopt=optional_metadata_types=filelists as a broader fix.)
gcr
adwaita-icon-theme
google-noto-sans-fonts
# Firefox is the browser the operator is handed: it is free software,
# which matters for an image that pitches exactly that. Chrome stays
# INSTALLED and is not optional — kldload-chrome-app opens ten tool
# windows through it, and Bob's voice needs the Web Speech API, which
# is Chromium-only. So Chrome is an internal component here rather than
# the default browser: it lives in the Apps folder, not the dock.
firefox
mesa-dri-drivers
pipewire
wireplumber

# Container runtime (required for Open WebUI)
podman
