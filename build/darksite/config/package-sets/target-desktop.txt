# GNOME desktop profile packages
gnome-shell
gnome-session
gnome-control-center
gnome-settings-daemon
gdm
nautilus
gnome-terminal
gnome-text-editor
gnome-tweaks
file-roller
gnome-system-monitor
gnome-keyring
# gcr provides /usr/libexec/gcr-ssh-askpass which gnome-keyring file-requires.
# dnf5 doesn't auto-load filelists, so file-requires can't be satisfied via
# Provides resolution alone. Explicit install side-steps it. (bootstrap.sh
# also passes --setopt=optional_metadata_types=filelists as a broader fix.)
gcr
adwaita-icon-theme
google-noto-sans-fonts
# firefox intentionally NOT listed — google-chrome-stable is the only kldload
# browser (Task #47). Chrome renders the web GUI (its Chromium-only Web Speech
# API is what powers Bob's hot mic) and is installed from Google's repo by
# build-iso.sh. firstboot also removes firefox if gnome-shell pulls it in.
mesa-dri-drivers
pipewire
wireplumber

# Container runtime (required for Open WebUI)
podman
