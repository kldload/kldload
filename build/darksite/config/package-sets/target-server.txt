# Server profile extras (beyond target-base)
chrony
socat
nmap-ncat
bind-utils
mtr
sysstat
acl
attr
iperf3

# SSH automation (kube-cluster cloud-init password fallback)
sshpass

# Container runtime (required for Open WebUI)
podman

# Observability (klab estate dashboard)
golang-github-prometheus
golang-github-prometheus-node-exporter
grafana

# KVM Host (optional checkbox)
qemu-kvm
libvirt-daemon
libvirt-daemon-driver-storage
libvirt-client
virt-install
bridge-utils
edk2-ovmf
swtpm
swtpm-tools
python3-virt-firmware
libguestfs-tools

# ksmtuned — KSM page sharing across guests on KVM hosts. Installed by
# k_profile_optional_packages under the KVM gate, enabled by profiles.sh
# and firstboot. Same name on every family that has it (2026-09-04).
ksmtuned

# mkisofs (NoCloud seeds) and jq for kfire and vmxplore on a KVM host (2026-09-05)
xorriso
jq
