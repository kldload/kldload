# GNOME desktop profile packages
# Individual packages instead of task-gnome-desktop to avoid gnome-snapshot
# → gstreamer1.0-plugins-bad → libfluidsynth3 → sf3-soundfont-gm dep failure
gnome-shell
gnome-session
gnome-control-center
gnome-settings-daemon
gdm3
nautilus
gnome-terminal
gnome-text-editor
# Ubuntu uses eog, not loupe (Debian)
eog
gnome-tweaks
file-roller
gnome-system-monitor
adwaita-icon-theme
fonts-cantarell
gvfs
gvfs-backends
gnome-keyring
# Ubuntu's firefox is a snapd transitional package — use GNOME Web instead
epiphany-browser
# Xorg fallback — Wayland fails on virtual GPUs (KVM, VMware)
xserver-xorg
xserver-xorg-video-qxl
xserver-xorg-video-vesa
ubuntu-wallpapers
fonts-liberation
fonts-noto-core

# Container runtime (required for Open WebUI)
podman

# Flatpak runtime — firstboot installs Steam via Flathub (com.valvesoftware.Steam).
# Absent through 1.3.1: the closure dropped it, install pass 1 --skip-broken
# swallowed it, and every desktop shipped Steam-less with no log trail.
flatpak

# Clipboard bridges for kldload-sysdiag copy-paste (891a2d5 piped copies
# through wl-copy/xclip but only Arch/vdi package lists carried them —
# on Fedora/EL/Debian desktops the fix was inert; same names on all four).
wl-clipboard
xclip
